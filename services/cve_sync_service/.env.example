# Voltaxe CVE Sync Service - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# ⚠️  DO NOT commit .env to version control!

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string (REQUIRED for production)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://voltaxe_user:YOUR_PASSWORD_HERE@localhost:5432/voltaxe_cve

# SQLite (for development only - NOT recommended for production)
# DATABASE_URL=sqlite:///./voltaxe_cve.db

# ============================================
# NIST National Vulnerability Database API
# ============================================
# Get your free API key at: https://nvd.nist.gov/developers/request-an-api-key
# Rate Limits:
#   - Without API key: 5 requests per 30 seconds (public rate limit)
#   - With API key: 50 requests per 30 seconds (authenticated rate limit)
NVD_API_KEY=your-nvd-api-key-here

# ============================================
# CVE Synchronization Settings
# ============================================
# How often to sync CVE data (in hours)
SYNC_INTERVAL_HOURS=6

# Maximum records to fetch per API request
MAX_RECORDS_PER_REQUEST=2000

# ============================================
# Performance & Rate Limiting
# ============================================
# Delay between API requests (in seconds) to respect rate limits
RATE_LIMIT_DELAY_SECONDS=6

# Number of CVE records to commit to database at once (batch processing)
BATCH_COMMIT_SIZE=100

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# ============================================
# Environment Settings
# ============================================
ENVIRONMENT=development  # Options: development, staging, production
DEBUG=false  # Set to false in production

# ============================================
# Email Alerts (Optional)
# ============================================
# Send notifications for critical CVEs
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password-here
# SMTP_USE_TLS=true
# ALERT_EMAIL=admin@yourdomain.com

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit this file with real secrets to version control
# 2. NVD API key is free but should be kept confidential
# 3. Rotate API keys every 90 days for security best practices
# 4. Monitor API usage to detect unauthorized access
# 5. Use different keys for dev/staging/production environments
# 6. Store production secrets in a secure vault (AWS Secrets Manager, HashiCorp Vault, etc.)

# ============================================
# QUICK START
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Get NVD API key: https://nvd.nist.gov/developers/request-an-api-key
# 3. Fill in your NVD_API_KEY and DATABASE_URL
# 4. Verify .env is in .gitignore: grep -q "^\.env$" ../.gitignore && echo "✓ Safe" || echo "✗ Add .env to .gitignore!"
# 5. Start the sync service: python3 cve_sync.py
